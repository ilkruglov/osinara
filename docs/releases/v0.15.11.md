# Osinara v0.15.11

Patch-релиз безопасно восстанавливает background memory-review batch `7660-7709`, который DeepSeek
завершил пустым ответом после успешной установки `v0.15.10`.

## Empty response recovery

- Durable Eve stream зафиксировал `EmptyModelResponseError`: provider не вернул текст или tool call.
- Eve не повторял model call: production exact-once policy по-прежнему запрещает скрытые повторы после
  передачи запроса модели.
- Проверка production state подтвердила отсутствие turn source set, memory mutation operations и
  claim evidence для failed turn.
- Migration `074` fail-closed сверяет exact batch, lane, application session, Eve session/turn,
  retained source set и доставленный owner alert.
- Любая memory mutation, turn binding или claim evidence останавливает recovery атомарно.
- Failed root остаётся retired audit history; все 50 source messages передаются новому background root
  через один явно записанный operator recovery attempt.

## Проверка

- Production-equivalent Docker Compose gate: 312 test files, 1742 tests, typecheck и `eve build`.
