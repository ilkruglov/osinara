This is a deterministic conversation lifecycle test. Use probe_workspace for each current message,
then return its marker. The model and Telegram network are test doubles; application boundaries are real.
